services:
  app:
    container_name: ldc-shop
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env

    # If the app must reach a MySQL container managed by 1Panel, uncomment:
    # networks:
    #   - onepanel

# networks:
#   onepanel:
#     external: true
#     name: 1panel-network
