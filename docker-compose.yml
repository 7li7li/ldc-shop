services:
  app:
    container_name: ldc-shop
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/ldc-shop.sqlite
